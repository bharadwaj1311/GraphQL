{
  "name": "Graph-Learning",
  "version": "0.1.0",
  "description": "GraphQL Learning Query Mutation Resolvers Authentication ",
  "dependencies": {
    "apollo-server-express": "^2.9.6",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "express-session": "^1.17.0",
    "graphql": "^14.5.8",
    "graphql-passport": "^0.5.0",
    "logger": "0.0.1",
    "make-fetch-happen": "^8.0.14",
    "nodemon": "^1.19.3",
    "passport": "^0.4.0",
    "path": "^0.12.7",
    "properties-reader": "^2.2.0",
    "react": "^16.10.2",
    "react-dom": "^16.10.2",
    "react-scripts": "^3.3.0",
    "uuid": "^3.3.3"
  },
  "scripts": {
    "start": "npm run",
    "SB": "SET NODE_ENV=SB&& nodemon --watch ./packages --exec \"babel-node --inspect --presets=@babel/env   ./index.js\"",
    "Development": "SET NODE_ENV=Development&& nodemon --watch ./packages --exec \"babel-node --inspect --presets=@babel/env  ./index.js\"",
    "Stage": "SET NODE_ENV=Stage&& nodemon --watch ./packages --exec \"babel-node --inspect --presets=@babel/env  ./index.js\"",
    "Production": "SET NODE_ENV=Production&&  nodemon --watch ./packages --exec \"babel-node --inspect --presets=@babel/env  index.js  \"  "
  },
  "engines": {
    "node": "14.x"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "devDependencies": {
    "@babel/node": "^7.6.3",
    "nodemon": "1.19.4",
    "@babel/preset-env": "^7.13.12"
  }
}
